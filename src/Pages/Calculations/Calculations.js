import React from 'react';

const Calculations = ({ review }) => {
    let sum = 2;
    const { overall } = review
    const x = parseFloat(overall)

    return (
        <div>

        </div>
    );
};

export default Calculations;